# Full-app audit — 2026-08-16

Requirements gathered from three sources and checked against the running app
and the code:

1. **`prd.md`** — FR-001…FR-018 and the non-functional requirements. The
   contract as originally written.
2. **`product-roadmap.md`** — 71 build tasks, all ticked.
3. **This chat** — everything decided after the PRD was written: the skeu
   design mode, the global light/dark switch, the typeface setting, the
   settings-layout parity pass, the shared toggle, and the Mononote pin.
   **None of it is in the PRD**, which is itself a finding.

Status key: `?` not yet checked · `OK` verified · `DRIFT` spec and app
disagree · `BUG` broken · `RISK` works but the construction is fragile.

---

## Phase 1 — Spec drift

The PRD is the contract. Where the app has moved on, either the app is wrong
or the PRD is stale — both are findings, and they are cheapest to settle
first because everything below is measured against the answer.

| # | Item | Status |
|---|---|---|
| D1 | FR-018 requires `(empty)` states. Founder removed them (2026-08-14). | **PRD updated** |
| D2 | FR-012 says "one photo per task". App stores many (`TaskPhoto` records). | **PRD updated** |
| D3 | FR-006 says "camera glyph attaches a photo to a task created from current text". Founder changed this (2026-08-14) so the plus commits the current task and opens the photo menu for a NEW one. | **PRD updated** |
| D4 | A second complete design language exists. | **PRD updated — FR-019** |
| D5 | Nothing covered the pinned task / Live Activity. | **PRD updated — FR-021** |
| D6 | Settings gained Theme, Typeface, Design, Light & dark, Tab names, Workspaces. | **PRD updated — FR-020** |
| D7 | FR-009 said undo is "persistent until replaced"; it has always retired after 6s. | **PRD updated** |

## Phase 2 — PRD requirements, still in force

| # | Requirement | Check | Status |
|---|---|---|---|
| F01 | FR-001 date buckets | unit tests exist and pass; boundary cases | ? |
| F02 | FR-002 swipe = one step, thresholds, rubber-band | both looks, all four tabs | ? |
| F03 | FR-003 context menu entries + order | both looks | ? |
| F04 | FR-004 hold→menu vs hold-drag→reorder | both looks | ? |
| F05 | FR-005 placement engine + `overduePlaced` | unit tests | ? |
| F06 | FR-006 inline add, Return commits + dismisses | both looks | **BUG** — draft survives commit |
| F07 | FR-007 inline edit, commit/revert | both looks | **OK** |
| F08 | FR-008 completion + archive grouping | both looks | ? |
| F09 | FR-009 undo, single level, persistent | both looks | ? |
| F10 | FR-010 Win95 components + §9 prohibited list | Win95 | **BUG** (1 fixed, 3 transitions reported) |
| F11 | FR-011 motion: position animates, appearance instant | both looks | ? |
| F12 | FR-012 photos: downscale, thumbnail, viewer | both looks | ? |
| F13 | FR-013 CloudKit: status row, no login UI | — | **OK** (two-device round-trip still open) |
| F14 | FR-014 Settings / Archive / About | both looks | ? |
| F15 | FR-015 stepped Dynamic Type (2×/3×/4×) | both looks, incl. new pin control | ? |
| F16 | FR-016 VoiceOver labels + custom actions | both looks, incl. pin | ? |
| F17 | FR-017 icon + launch screen | — | ? |
| N1 | Tap targets ≥ 44pt — incl. the new pin glyph | both looks | **OK** — 3 violations found and fixed |
| N2 | Meaning never by colour alone — the pin is a shape change, not just a tint | both looks | ? |
| N3 | Offline / force-quit / relaunch: no data loss | — | ? |

## Phase 3 — Founder directions from this chat

| # | Direction | Status |
|---|---|---|
| C1 | Skeu mode is feature-complete against the Win95 mode | ? |
| C2 | Light/dark is GLOBAL and independent of design | **OK** |
| C3 | Three settings stacked: typeface, design, light&dark | **OK** |
| C4 | Settings order and positioning identical in both looks | **OK** |
| C5 | Settings toggles are the home screen's toggle, one construction | ? |
| C6 | Field rows use the toggle's trough + inner-pill heights | ? |
| C7 | Theme colours fill the whole pill | ? |
| C8 | Every settings toggle glides (except Design — no target exists) | ? |
| C9 | Typeface setting applies everywhere in the skeu look | ? |
| C10 | Settings stay open across a design switch | **OK** |
| C11 | Pin: circle left of the camera, editing-only unless it holds | ? |
| C12 | Pin: exactly one, app-wide, replace asks first | ? |
| C13 | Pin: Live Activity in the active design, tick button | ? |
| C14 | Win95 dark palettes | **OK** |

## Phase 4 — Code construction

Not bug-hunting — construction. Areas where this codebase has a stated
principle that can be checked mechanically:

| # | Check | Status |
|---|---|---|
| Q1 | No hardcoded colours outside `Theme/` and `SkeuKit/Theme/` (CLAUDE.md rule 2) | ? |
| Q2 | No `cornerRadius` without `.continuous`; radii from `SkeuRadius` | **RISK** — continuous clean, 3 off-token radii |
| Q3 | Shadows use `skeu.shadow`, never black/grey; always paired | ? |
| Q4 | One depth from the ladder per object, no in-between values | **RISK** — ladder bypassed; two light sources |
| Q5 | Duplicated construction between the two looks that should be shared | ? |
| Q6 | Dead code / unused tokens left behind by this session's rewrites | **OK** — 3 more removed |
| Q7 | Force-unwraps, `try!`, and silent `try?` that can swallow real failures | ? |
| Q8 | Store mutations that bypass `commit()` / miss `revision` | ? |
| Q9 | `@Observable` reads placed where SwiftUI cannot see them (the stale-colour class of bug) | ? |
| Q10 | Tests: what exists, what passes, what is untested | **RISK** — 52 pass, app target has no tests |

## Phase 5 — Findings and fixes

### Fixed

**Q1-a — `SkeuWorkspacePill` painted itself with `Color.white`** (three call
sites: the chevron stroke, the pill's foreground, the dropdown rows). The
struct did not read `\.skeu` at all, which is why it reached for a literal.
This broke the design system's own acceptance test — "any screen must re-skin
correctly by changing only `.skeuTheme(...)`" — and CLAUDE.md lists
`hardcoded Color.white` on the reject-on-sight list. Now `skeu.ink` /
`skeu.inkMuted`.

**Q1-b — `Win95PinReplaceDialog` used `Color.black` for its scrim** (written
in this session). A scheme whose darkest tone is not black would have shown a
black veil over its own colours. Now `Win95.darkShadow`.

### Resolved by founder decision

**A1 — The light skeu palettes failed their own contrast requirement.**
Founder chose: light means light, text black (2026-08-16). Implemented — see
the "Light mode is light" commit. Cream now measures 10.9:1; the four derived
themes are lifted at their single base. Original analysis kept below.

`cream` is `ink: .white` on `canvas: #AA8360`. Measured: **3.42:1**.
`inkMuted: #F2E4D5` on the same canvas: **2.72:1**. `SkeuPalette.swift` itself
annotates `inkMuted` with "≥ 4.5:1 required", and the design system's
accessibility section repeats it. Primary text sits below the threshold for
secondary text.

This is NOT a bug in the sense of "someone made a mistake" — the whole look
was transcribed from a Figma frame that was dark, and the light variant kept
the same white-on-brown relationship at a lighter canvas. It reads as
intended, it just does not meet the contrast bar the project set for itself.

Three ways out, none of them a small edit:
1. Darken `ink` to a deep brown in the light palettes and keep the canvas.
   Correct by the numbers; changes the light theme's character completely.
2. Darken the canvas until white clears 4.5:1 (needs roughly #8A6A4C or
   darker). Keeps the white-on-brown look; makes "light" barely lighter
   than "dark".
3. Accept it and amend the design system to state the real target.
   Honest, but it is a real accessibility regression for anyone using it.

Affects all five themes' light variants, not just cream.

**Q7-a — the package stopped building for macOS**, and with it the whole test
suite. `PinnedTaskActivity` and `PinnedTaskIntents` guarded on
`canImport(ActivityKit)` / `canImport(AppIntents)`. Both modules ARE importable
on macOS — every type inside is marked unavailable instead — so the guard
compiled in and then failed on the protocol. `Package.swift` targets macOS for
the future Mac app, so `swift test` died before running a single test.
Narrowed to `#if os(iOS)`. **52 tests in 12 suites pass again.** Introduced
earlier the same day; caught here.

### Verified

| Check | Result |
|---|---|
| F01 FR-001 buckets · F05 FR-005 placement | 52 unit tests pass |
| F02 FR-002 swipe = one step | skeu light: right-swipe deferred to Tomorrow |
| F09 FR-009 undo | panel appears with correct text and an Undo control |
| C7 theme colours fill the pill | confirmed |
| C11–C13 pin | control, replace dialog, Lock Screen card, both looks |
| N2 meaning not by colour alone | the pin changes SHAPE (struck ring → solid core), not just tint |
| Light mode | genuinely light, near-black ink, rim and shadow re-derived |

### Round 2 — findings

**Q6-a — dead code from today's own rewrites (fixed).** `SkeuSettingsView.G`
still carried `cardRadius`, `cardRim`, `cardPad`, `troughPad`, `pillPadV`,
`icon`, `pillSmall` and `circle` — all zero references since the outer cards
and the old option pills were replaced by `SkeuSegment` / `SkeuRowButton`.
Plus six computed vars (`pillSmallH`, `circleSize` in three structs each) and
two unused `pillH` copies. Removed.

**A2 — a photo cannot be deleted in the skeu look. REPORTED, not fixed.**
`PhotoViewer` (Win95) takes `onRemove` and calls `store.removePhoto`.
`SkeuPhotoViewer` takes only a close callback — `store.removePhoto` has
exactly one caller in the whole app, and it is the Win95 viewer. So a photo
attached in the skeu look can never be removed without switching design.
Fails C1 (parity). Not fixed here because WHERE the control goes and what it
looks like in that viewer is a design decision, not a mechanical one.

**A3 — FR-003's photo menu entries were never built, in either look.** The
spec asks for `Attach photo` / `Replace photo` / `Remove photo` in the row
menu. Neither menu has them; photos are added by the camera glyph in the
editing row instead, which post-dates the requirement. This is spec drift,
not a bug — except that it leaves A2 with no second route.

**Q3, Q8 — clean.** Every shadow resolves through the palette; the two
`SkeuCard` calls go via a local helper. The four store functions that write
without `commit()` are a read-only query, a private helper whose callers all
commit, and two debug seeders.

### Round 3 — verified

| Check | Result |
|---|---|
| F03 FR-003 row menu | entries and order correct in skeu; photo entries absent in BOTH looks (A3) |
| F04 FR-004 hold → menu | long-press opens the menu, no drag started |
| F18 empty state | withdrawn as specified — bare add row, no `(empty)` |
| Q5 shared construction | ChipFormat, Placement, RowGestureView and all three coordinators are used by both looks — logic is shared, not duplicated |
| Q9 observation | the `store.workspaces()` reads inside parent bodies all pass through `revision`, so the dependency is registered |
| A2 photo delete | CLOSED — delete control added to the skeu viewer |

### Round 4 — N1, F10, Q4

**N1 — three tap targets under 44pt. All three fixed.** The codebase already
has the right idiom in six places (`SkeuControl.minTouch` as an outer frame
around a smaller glyph box); these three had the visual size and no target.

| Site | Was | Now |
|---|---|---|
| `SkeuRootView.swift:1411` photo-viewer Delete + Close | 37×37 | glass stays 37, target 44 |
| `SkeuRootView.swift:612` Undo | 30pt tall | pill stays 30, target 44 in a 46pt bar |
| `SkeuSecondaryScreens.swift:267` Privacy policy | 38pt tall | pill stays 38, target 44 |

Worth noting the first one is the delete control added earlier today to close
A2 — the parity fix shipped a fresh rule-9 violation. Nothing visible changed
in any of the three; only the hit area grew. Build passes.

**F10 — Win95 against design.md §9.**

*Fixed:* `TaskRowView.swift:157` veiled the photo viewer in
`Color.black.opacity(0.55)`. This is Q1-b's twin — the same bug, a second
site, missed in round 1 because that pass found the pin dialog and stopped.
Now `Win95.darkShadow`.

*Reported, not fixed — §9 bans "fade, scale, dissolve, or cross-fade
transitions of any kind", and FR-011 says appearance is instant:*

- `Win95Menu.swift:168` and `RootView.swift:127` — both menus appear with
  `.transition(.scale(0.86).combined(with: .opacity))`. Scale AND fade, two
  of the four named types, in the two most-used menus in the app.
- `LaunchCover.swift:39` — `.transition(.opacity)`, a fade.

Not fixed here because removing them changes how the app feels on every menu
open, and they were written deliberately (both carry anchor logic). Either
they go, or §9 gets an exception clause. Founder's call.

*Spec drift, not a violation:* `RootView.swift:74` slides the list on tab
switch. §9 does not ban slides, but FR-011 says tab switches have **no**
transition. The code comment argues the case well (furniture stays, content
moves). The PRD should say so.

*Deliberate exception, recorded so it stops looking like a bug:* §9 bans the
word "Windows" in user-facing strings; About carries "Windows 95 is a
trademark of Microsoft Corporation" in both looks. That is the trademark
attribution and it stays.

**Q4 — the depth ladder is bypassed, and there are two light sources.**

`SkeuDepth` defines six rungs with paired ambient+contact shadows. It reaches
the screen through `skeuSurface`, which has **three** call sites. The skeu
look is actually built from `skeuGlass` (15), `skeuTrough` (6) and
`SkeuCard` — each carrying its own hand-transcribed Figma shadow stack that
is on no rung: SkeuGlass casts three shadows, SkeuCard two at radius 68 and
124. Plus two screen-level sites (`SkeuMenu.swift:92`,
`SkeuSecondaryScreens.swift:134`) that hand-roll a pair inline. Every one is
tinted through `skeu.shadow` and scales with `shadowIntensity`, so Q3 still
holds — but "pick exactly one depth from the ladder" is not what the look
does, and the ladder is not the source of truth CLAUDE.md rule 7 says it is.

Sharper, and the reason this matters beyond bookkeeping: **the x offsets
disagree with each other.** The doc puts the light at 10 o'clock, vector
`(-0.25, -1.0)` — shadows should fall to the lower **right**, x positive.

| Source | x offset | Implied light |
|---|---|---|
| `SkeuDepth` ladder, `SkeuGlass` | `0` | directly above |
| `SkeuCard` | `-16.5`, `-63.9` | upper **right** |
| `SkeuMenu`, `SkeuSecondaryScreens` | `-4`/`-12`, `-6.5`/`-25.4` | upper **right** |

No site casts to the right. So nothing matches the documented vector, and
glass objects and card objects do not match each other — a pill lies under a
light from straight above while the card behind it is lit from 2 o'clock.
That is rule 6's "never invert this per component", and it is invisible until
the two sit edge to edge, which is exactly what the settings screen does.

Cheapest resolution is almost certainly to amend the doc to the light the
Figma frames were actually drawn under and zero the disagreement in
`SkeuGlass`/`SkeuDepth`, not to re-sign every transcribed shadow. Either way
it is a founder decision, since it changes the look.

### Round 5 — Q2, Q6, Q10, and a simulator pass

**Q2 — `.continuous` is clean; the radius TOKENS are not.** Every
`cornerRadius:` in the app carries `style: .continuous` (three apparent
misses were line wraps). But three radii are off-token: `SkeuCard.swift:30`
uses a raw `123.262 * k` transcribed from Figma, `SkeuSecondaryScreens.swift:22`
defines `cardRadius = 30` (between `xl` 28 and `xxl` 40, on no rung), and one
pill computes `rowHeight / 2` where `SkeuRadius.pill` exists. Same shape as
Q4: the tokens are right, the principal surfaces route around them.

Sharper: **`nestedRadius` has exactly one call site in the whole app**
(`Seam.swift:24`). CLAUDE.md rule 4 calls concentric corners mandatory for
nested children. One use is not a system.

**Q6 — three dead declarations, removed.** `RootView.swift` injected
`@Environment(SyncStatus.self)` and never read it; `SkeuRootView` carried an
unused `topBarHeight`; `SkeuSettingsView` carried an unused `drop(_:)` helper
(the live copy is SkeuMenu's). Build passes.

**Q10 — 52 tests, 12 suites, all passing — and all of them pure logic.**
Coverage is `Bucket`, `DateEngine`, `Placement`, chip labels, archive
visibility, the locked menu-destination table. Untested: the entire app
target. `TaskStore`, `AppSettings`, `ImageImport`, `PinCoordinator`,
`LiveActivityController`, `SyncStatus` and `EditingCoordinator` have **no
test target at all** — `xcodebuild -list` shows only `shove95` and
`ShoveWidgets`. Every bug this audit has found sits in exactly that
untested half.

#### Verified in the simulator (iPhone 17 Pro, iOS 26.5)

| Check | Result |
|---|---|
| F06 inline add | Return commits, inserts no line, add row resets and follows down |
| F07 inline edit | Return commits; moving focus to another row commits; empty reverts (`TaskStore.editTitle` guards empty) |
| F02 swipe, Tomorrow | right-swipe moved one step, Tomorrow → Week, not two |
| F09 undo | panel reads "Tsw → Week" with an Undo control, retires after 6s |
| F13 CloudKit | "iCloud: not signed in" status row, no login UI anywhere |
| F14 Settings/Archive/About | all three present and reachable in both looks |
| F08 archive | keeps its `(empty)` as FR-018 specifies (grouping unverified — no aged data) |
| C2 light/dark global | switched Dark with design on Win95; the axes are independent |
| C3 three settings stacked | Typeface, Design, Light & dark, in that order under Theme |
| C4 settings parity | same order, same positions, same controls in both looks |
| C10 open across a switch | settings stayed open, same scroll position, same control under the finger |
| C11 pin control | circle left of the camera, appears only while editing |
| C14 Win95 dark | palette holds, and the bevel ramp is NOT inverted — the thing §9 actually protects |
| F10 red | red appears only on the Important task |

#### New findings

**BUG — the add row keeps its draft after committing, and the title loses a
character.** Reproduced twice. When the text and the Return arrive in one
input batch, `commitAdd()` creates the task but `draft` is left holding the
typed text: the add row then renders as what looks like an ordinary task,
with an unchecked circle, **in every tab**, and there is no add row left to
type into. Typing `Rep1⏎` produced a task titled `Re1` — a character was
eaten as well.

Mechanism: `returnCommittingAdd` strips the newline, writes `draft`, and
calls `commitAdd()`, which clears `draft` and defers the store write. UIKit
then writes its own buffer back through the same binding, takes the
`guard new.contains("\n") else { draft = new; return }` branch, and restores
the draft the commit had just cleared.

Typing the text and the Return as two separate events does NOT reproduce it,
which is why this survived — but batched input is not exotic: paste, dictation,
autocorrect replacement and predictive-bar insertion all deliver it, and so
does a fast typist. `AddRowView` (Win95) is the same construction line for
line, so both looks carry it.

Not fixed here. The comments on both copies record two earlier rounds of
on-device debugging ("why intercepting only one of them fails on device"),
and a race fix I can only verify through synthetic input is exactly the kind
of change that regresses the path already paid for. The safe shape is
probably to pass the title into `commitAdd(title:)` instead of re-reading
`draft`, and to re-clear `draft` in the deferred `Task` — but that wants a
device check.

**§9 — "Windows 95" is a control label, not just the trademark line.** The
Design segment is labelled `Windows 95` in both looks. §9 bans the word in
any user-facing string. The About disclaimer is a defensible legal exception;
a segment label is a choice. Either §9 gets amended or the label becomes
something like `95` / `Classic`.

**The skeu secondary screens scroll under the status bar.** Settings and
Archive have no safe-area inset and no scrim: scrolled content passes behind
the clock and the Dynamic Island, and the "Settings" title and the Design
segment collide with them legibly. The Win95 window does not — its title bar
starts below the status bar. Two looks, two behaviours, and the founder sees
this on the first scroll of the settings screen.

*(The simulator's store now carries this pass's throwaway rows — `RsgXXX`,
`Sfs-edited`, `Audit F06`, `Re1`, `Tsw`. Simulator only.)*

### Still open

Phase 2: F02 in Week and General, F08 grouping by day, F11 motion, F12
photos, F15 Dynamic Type, F16 VoiceOver, F17 icon and launch, N3
offline/force-quit. Phase 3: C1 full parity sweep, C5, C6, C8, C9.

---

## Known limits of this audit

- **Hardware keyboard Return** and **keyboard lifting** cannot be exercised in
  the MCP simulator — carried over from earlier in the project, still open.
- **The Live Activity tick button** could not be tapped: the simulator
  re-shows the "Allow Live Activities" prompt on every lock and it covers the
  card. Code path is in place, unverified.
- **Two-device CloudKit round-trip** (FR-013 acceptance) needs two devices.
- **Instruments performance targets** (120fps, cold launch < 1.0s) need a real
  device and a profiling run.
